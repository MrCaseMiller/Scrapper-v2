[build]
builder = "nixpacks"
buildCommand = "cd polymarket-sim/web && pip install -r requirements.txt"

[deploy]
startCommand = "cd polymarket-sim/web/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
